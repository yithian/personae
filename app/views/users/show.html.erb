<p>
  <fieldset>
  <legend><b><%=h @user.name %>:</b></legend>
  <table>
	<%= render(:partial => "character", :collection => @user.characters) %>
  </table>
  </fieldset>
</p>

<%= link_to 'Edit', edit_user_path(@user) if edit_user?(@user) %>
<%= link_to 'Back', users_path %>
