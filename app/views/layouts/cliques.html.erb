<!DOCTYPE html>
<html>
<head>
  <meta http-equiv="content-type" content="text/html;charset=UTF-8" />
  <title>
    <%= case params[:controller]
      when "characters" then @character.name if @character and @character.name + " : "
      when "cliques" then @clique.name if @clique and @clique.name + " : "
      when "ideologies" then @ideology.name if @clique and @ideology.name + " : "
    end %>
    City of Darkness
  </title>

  <%= javascript_include_tag :defaults %>
  <%= stylesheet_link_tag 'custom' %>
  <%= csrf_meta_tag %>
</head>
<body>

<div id="column">
  <div id="header">
    <h1>City of Darkness</h1>
    
    <ul class="menu">
      <li <%= 'class="selected"' if params[:controller] == "characters" %> ><%= link_to "Characters", :controller => :characters %></li>
      <li <%= 'class="selected"' if params[:controller] == "cliques" %> ><%= link_to "Cliques", :controller => :cliques %></li>
      <li <%= 'class="selected"' if params[:controller] == "ideologies" %> ><%= link_to "Ideologies", :controller => :ideologies %></li>
      <li <%= 'class="selected"' if params[:controller] == "users" %> ><%= link_to "Account", :controller => :users, :action => :show, :id => session[:user_id] %></li>
      <% if session[:user_id] == :logged_out or session[:user_id] == nil %>
        <li <%= 'class="selected"' if params[:controller] == "admin" %> ><%= link_to "Log in", :controller =>  :admin, :action => :login %></li>
      <% else %>
        <li><%= link_to "Log out", :controller => :admin, :action => :logout %></li>
      <% end %>
    </ul>
  </div>
  
  <div id="content">
    <div id="notice"><%= flash[:notice] %></div>
    <%= yield %>
  </div>
</div>

</body>
</html>
