<!DOCTYPE html>
<html>
<head>
  <meta http-equiv="content-type" content="text/html;charset=UTF-8" />
  <title>
    <%= case params[:controller]
      when "chronicles" then @chronicle.name + " : " if @chronicle and @chronicle.name
      when "characters" then @character.name + " : " if @character and @character.name
      when "cliques" then @clique.name + " : " if @clique and @clique.name
      when "ideologies" then @ideology.name + " : " if @clique and @ideology.name
    end %>
    City of Darkness
  </title>

  <%= javascript_include_tag :defaults %>
  <%= stylesheet_link_tag 'custom' %>
  <%= csrf_meta_tag %>
</head>
<body>

<div id="column">
  <div id="header">
    <h1>City of Darkness</h1>
    
    <ul class="menu">
      <li <%= "class='selected'" if params[:controller] == "characters" %> ><%= link_to "Characters", characters_path %></li>
      <li <%= "class='selected'" if params[:controller] == "cliques" %> ><%= link_to "Cliques", cliques_path %></li>
      <li <%= "class='selected'" if params[:controller] == "ideologies" %> ><%= link_to "Ideologies", ideologies_path %></li>
        <li <%= "class='selected'" if params[:controller] == "chronicles" %> ><%= link_to "Chronicles", chronicles_path %></li>
      <% if user_signed_in? %>
        <li<%= "class='selected'" if params[:controller] == "users" %> ><%= link_to "Account", edit_user_registration_path %></li>
        <li><%= link_to "Log out", destroy_user_session_path %></li>        
      <% else %>
        <li <%= 'class="selected"' if params[:controller] == "admin" %> ><%= link_to "Log in", new_user_session_path %></li>
      <% end %>
    </ul>
  </div>
  
  <div id="content">
    <div id="notice"><%= flash[:notice] %></div>
    <div id="alert"><%= alert %></div>
    <%= yield %>
  </div>
</div>

</body>
</html>
