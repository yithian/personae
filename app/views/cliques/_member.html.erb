<% if member.show_name_to_user?(current_user.id) and member.show_clique_to_user?(current_user.id) %>
  <tr>
    <td><%= link_to member.name, member %></td>
    <td><%= link_to member.ideology.name, member.ideology if member.show_clique_to_user?(current_user.id) %></td>
  </tr>
<% end %>
