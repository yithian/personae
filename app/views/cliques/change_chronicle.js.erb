$('list').update('<%= escape_javascript(render(:partial => "list")) %>');

$('new_link').replace('<%= escape_javascript(link_to "New clique", new_clique_path(:chronicle_id => current_user.selected_chronicle.id), :id => "new_link") %>');
