$('list').update('<%= escape_javascript(render(:partial => "list")) %>');

$('new_link').replace('<%= escape_javascript(link_to "New clique", {:controller => "cliques", :action => "new", :chronicle_id => current_user.chronicle_id}, :id => "new_link") %>');
