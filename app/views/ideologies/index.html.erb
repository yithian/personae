<table>
  <tr>
    <th>Name</th>
  </tr>

  <% @ideologies.each do |ideology| %>
    <tr>
      <td><%= link_to ideology.name, ideology %></td>
    </tr>
  <% end %>
</table>

<%= link_to 'New ideology', new_ideology_path if session[:user_id] == User.find_by_name("Storyteller").id %>
