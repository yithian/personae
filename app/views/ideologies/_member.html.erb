<% if show_character?(member) and show_ideology?(member) %>
  <tr>
    <td><%= link_to member.name, member %></td>
    <td><%= link_to member.clique.name, member.clique if show_clique?(member) %></td>
  </tr>
<% end %>
