<%= form_for(@ideology) do |f| %>
  <% if @ideology.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ideology.errors.count, "error") %> prohibited this character from being saved:</h2>

      <ul>
      <% @ideology.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <h3>Name</h3>
  <p><%= f.text_field :name %></p>

  <%= f.submit 'Save' %>
<% end %>
