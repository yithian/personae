$('list').update('<%= escape_javascript(render(:partial => "list")) %>');

$('new_link').replace('<%= escape_javascript(link_to "New character", new_character_path(:chronicle_id => current_user.selected_chronicle_id), :id => "new_link") %>');
