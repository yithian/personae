$('list').update('<%= escape_javascript(render(:partial => "list")) %>');

$('new_link').replace('<%= escape_javascript(link_to "New character", {:controller => "characters", :action => "new", :chronicle_id => current_user.selected_chronicle_id}, :id => "new_link") %>');
