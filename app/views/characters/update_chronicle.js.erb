// update the hidden field in the form to save the chronicle_id
$('#character_chronicle_id').val('<%= @chronicle.id %>');

$('#character_clique_id').html('<%= options_for_select(@clique_list).gsub(/[\n]/, ' ').gsub(/[\']/, "&#39").html_safe %>');
