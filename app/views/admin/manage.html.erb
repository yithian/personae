<h4>Admins</h4>

<%= form_tag(manage_admins_path, :method => :put) do %>
<table>
  <% @users.each do |user| %>
  <tr>
    <td><%= check_box_tag "admin_ids[]", user.id, user.admin? %></td>
    <td><%= user.name %></td>
  </tr>
  <% end %>
</table>
<%= submit_tag "Save admins" %>
<% end %>
